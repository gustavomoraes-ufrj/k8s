apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: operator
  name: operator
  namespace: apps
spec:
  serviceAccountName: api-access
  containers:
  - image: nginx
    name: operator
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
